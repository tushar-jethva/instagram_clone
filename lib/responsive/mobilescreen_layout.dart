import 'package:flutter/material.dart';

class MyMobileScreenLayout extends StatelessWidget {
  const MyMobileScreenLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("This is mobile!"),
      ),
    );
  }
}