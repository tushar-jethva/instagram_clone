import 'package:flutter/material.dart';

class MyWebScreenLayout extends StatelessWidget {
  const MyWebScreenLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("This is web!"),
      ),
    );
  }
}
